name: router_controller
description: A new Flutter project.
version: 1.1.3
homepage:

environment:
  sdk: '>=3.5.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  universal_html: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

